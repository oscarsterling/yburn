{
  "name": "system-diagnostics",
  "description": "Collects disk usage, CPU, memory, uptime, and key process status",
  "version": "1.0",
  "match_keywords": ["disk", "cpu", "memory", "uptime", "system", "health", "diagnostic", "diagnostics", "load", "process"],
  "replaces_patterns": ["system health", "system diagnostic", "nightly diagnostic", "disk check", "health check"],
  "parameters": [
    {
      "name": "processes_to_check",
      "type": "list",
      "required": false,
      "default": [],
      "description": "Process names to verify are running"
    },
    {
      "name": "disk_threshold_pct",
      "type": "int",
      "required": false,
      "default": 80,
      "description": "Alert if any disk volume exceeds this usage percentage"
    }
  ],
  "output_format": "telegram_markdown",
  "requires": []
}
