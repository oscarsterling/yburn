{
  "name": "cron-health-report",
  "description": "Lists all cron jobs, counts OK/FAIL/disabled, reports health status",
  "version": "1.0",
  "match_keywords": ["cron", "health", "report", "status", "jobs", "audit", "scheduled"],
  "replaces_patterns": ["cron health", "health report", "cron audit", "cron status"],
  "parameters": [
    {
      "name": "include_disabled",
      "type": "bool",
      "required": false,
      "default": true,
      "description": "Whether to include disabled jobs in the report"
    }
  ],
  "output_format": "telegram_markdown",
  "requires": []
}
